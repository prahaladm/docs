---
title: "Style Contract"
description: "Full reference for design tokens, style vocabulary, and theming rules."
---

This page should document the style side of the authoring contract: token categories (color, spacing, radius, shadow, typography), the style vocabulary an agent can use, and how modes/themes resolve at runtime.

<Note>
  TODO: generate from `list_style_vocabulary` and `get_style_enums`.
</Note>