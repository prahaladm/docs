---
title: "Backend Conventions"
description: "Naming, schema, and policy conventions used by NativeFlow's managed backend."
---

This page should document backend-level conventions: table/column naming, default policies created with `create_policy`, and how RLS is structured by default.

<Note>
  TODO: document default policy templates and naming conventions in detail.
</Note>