---
title: "Tool Catalog"
description: "Full reference for every MCP tool exposed by the NativeFlow Studio MCP server."
---

This page should be the exhaustive MCP tool reference: every tool grouped by area (project, design system, tables, components, translations, deployment), with parameters and return shapes.

<Note>
  TODO: generate this reference from `list_actions`/`get_action_schema` rather than hand-maintaining it, to avoid drift from the live server.
</Note>