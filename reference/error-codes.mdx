---
title: "Error Codes"
description: "Reference list of NativeFlow error codes and how to resolve them."
---

This page should list every error code an author (Studio user or agent) can hit — validation errors, backend errors, build errors — with a short description and remediation for each.

<Note>
  TODO: populate the table once error codes are finalized; keep in sync with `validate`/`validate_node` output.
</Note>