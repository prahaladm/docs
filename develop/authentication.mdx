---
title: "Authentication"
description: "Setting up sign-in and access policies for a NativeFlow app."
---

This page should cover configuring authentication for an app: sign-in methods, session handling, and row-level access policies that govern what signed-in users can read or write.

<Tabs>
  <Tab title="Studio">
    TODO: step-by-step instructions for enabling authentication and configuring access policies through the Studio backend panel.
  </Tab>
  <Tab title="Agent">
    TODO: show the equivalent agent flow using `create_policy` and `enable_rls` to configure authentication and access control.
  </Tab>
</Tabs>