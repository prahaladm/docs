---
title: "Cloud Functions"
description: "Writing and deploying server-side edge functions for custom backend logic."
---

This page should cover custom server-side logic that doesn't fit the standard table/API model: writing an edge function, deploying it, and invoking it from a component or another function.

<Tabs>
  <Tab title="Studio">
    TODO: step-by-step instructions for writing and deploying an edge function through the Studio backend panel.
  </Tab>
  <Tab title="Agent">
    TODO: show the equivalent agent flow using `deploy_edge_function` and `invoke_edge_function`.
  </Tab>
</Tabs>