---
title: "Styling and Theming"
description: "Applying design tokens, text styles, and light/dark theme modes to a project."
---

This page should cover applying the project's design system to components: color tokens, text styles, spacing/radius tokens, and switching or defining light/dark modes.

<Tabs>
  <Tab title="Studio">
    TODO: step-by-step instructions for applying theme tokens to a component and previewing light/dark modes in Studio.
  </Tab>
  <Tab title="Agent">
    TODO: show the equivalent agent flow using `design_system_set_color_tokens`, `create_theme_color`, and `create_text_style` to define and apply styling.
  </Tab>
</Tabs>