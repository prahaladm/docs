---
title: "Components and Layout"
description: "Composing components and layout containers to build a screen's UI."
---

This page should cover the building blocks available on the canvas — Button, Text, Input, Image, Card, Icon — and the layout containers (Box, Column, Row, Grid, Stack, ScrollView, List) used to arrange them.

<Tabs>
  <Tab title="Studio">
    TODO: step-by-step instructions for dragging components and layout containers onto a screen and adjusting props via the Studio inspector.
  </Tab>
  <Tab title="Agent">
    TODO: show the equivalent agent flow using `add_component_tree` and `create_component_spec` to compose a screen's UI programmatically.
  </Tab>
</Tabs>