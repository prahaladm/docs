---
title: "API Binding"
description: "Connecting components to backend tables and external APIs."
---

This page should cover binding a component (like a List or Card) to a live data source — a backend table or a registered external API — so it renders and updates with real data.

<Tabs>
  <Tab title="Studio">
    TODO: step-by-step instructions for binding a component to a data source using the Studio data-binding panel.
  </Tab>
  <Tab title="Agent">
    TODO: show the equivalent agent flow using `bind_api_to_component` and `register_table_api` to wire a component to a data source.
  </Tab>
</Tabs>