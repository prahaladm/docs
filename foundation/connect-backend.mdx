---
title: "Connect a Backend"
description: "Wiring a NativeFlow project to its managed backend or to an existing external backend."
---

This page should cover backend setup at the project level: using NativeFlow's managed backend (tables, policies, edge functions) versus connecting an existing external API, and what's needed before Develop \> Backend and Data becomes usable.

<Tabs>
  <Tab title="Studio">
    TODO: step-by-step instructions for connecting a backend from the Studio project settings panel.
  </Tab>
  <Tab title="Agent">
    TODO: show the equivalent agent flow using `get_backend_status` to check current state and `register_table_api` / `register_edge_function_api` to wire things up.
  </Tab>
</Tabs>