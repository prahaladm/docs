---
title: "Create a Project"
description: "Starting a new NativeFlow project from Studio or from an AI agent."
---

This page should walk through the first thing anyone does in NativeFlow: creating a project. Cover naming, initial template choice, and what gets scaffolded (empty design system, empty backend, a blank home screen).

<Tabs>
  <Tab title="Studio">
    TODO: step-by-step drag-and-drop instructions for creating a new project from the Studio dashboard (New Project button → template picker → initial screen).
  </Tab>
  <Tab title="Agent">
    TODO: show the equivalent agent flow using the `create_project` MCP tool, including required/optional parameters and the expected response shape.
  </Tab>
</Tabs>