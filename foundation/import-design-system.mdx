---
title: "Import a Design System"
description: "Bringing existing brand colors, typography, and components into a NativeFlow project."
---

This page should cover getting an existing brand or design system into NativeFlow so generated screens match it: colors, text styles, spacing, and any existing component library conventions.

<Tabs>
  <Tab title="Studio">
    TODO: step-by-step instructions for importing/defining a design system through the Studio design system panel (colors, modes, typography, components).
  </Tab>
  <Tab title="Agent">
    TODO: show the equivalent agent flow using `design_system_import_markdown` (or the relevant `design_system_set_*` tools) to bring a design system in programmatically.
  </Tab>
</Tabs>