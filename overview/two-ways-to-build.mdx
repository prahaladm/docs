---
title: "Two Ways to Build"
description: "Building visually in NativeFlow Studio versus building programmatically with an AI agent."
---

This page should lay out the two authoring paths side by side: a human dragging components onto the canvas in Studio, and an agent calling MCP tools like `create_project` or `design_upsert_screen`. Explain that these are two front ends to the same authoring contract, so teams can mix both — a developer can hand-polish a screen an agent scaffolded, and vice versa.

This page sets up the pattern used throughout Foundation and Develop, where every page shows the Studio steps and the equivalent agent tool call side by side.

<Note>
  TODO: add a comparison table (task → Studio action → MCP tool call).
</Note>