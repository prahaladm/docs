---
title: "System Architecture"
description: "How NativeFlow's canvas, backend services, and generated app code fit together."
---

This page should diagram the major pieces of a NativeFlow deployment: the Studio canvas and its document model, the managed backend (tables, policies, edge functions), the design system layer, and the code generation pipeline that turns the document model into a real mobile app.

Call out where the MCP server sits in this picture — it reads and writes the same document model that Studio edits, which is why both paths stay in sync.

<Note>
  TODO: add an architecture diagram (canvas/document model → backend → codegen → build output).
</Note>