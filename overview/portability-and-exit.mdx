---
title: "Portability and Exit"
description: "How to export your app's generated code and leave the platform if you ever need to."
---

This page should address the question architects ask before committing to a platform: what happens if we want to leave? Cover the generated-code export path (see Deploy \> Export Code), what's portable (the generated native/RN codebase) versus what's platform-specific (the visual document model, managed backend conveniences), and any lock-in tradeoffs a reader should evaluate.

<Note>
  TODO: get concrete numbers/examples on export format and how much manual rework is typically needed after an export.
</Note>