---
title: "Generate Builds"
description: "Producing installable iOS and Android builds from a NativeFlow project."
---

This page should cover the build pipeline: triggering a build, supported platforms, signing configuration, and where to download the resulting artifact.

<Note>
  TODO: document build time expectations and how signing credentials/certificates are managed.
</Note>