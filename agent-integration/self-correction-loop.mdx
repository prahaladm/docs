---
title: "Self-Correction Loop"
description: "How an agent should validate and fix its own changes before finishing a task."
---

This page should describe the recommended loop: make a change, call `validate`/`validate_node`, and repair errors before moving on, rather than assuming a tool call succeeded structurally as well as syntactically.

<Note>
  TODO: add a worked example of an agent catching and fixing a broken binding via this loop.
</Note>