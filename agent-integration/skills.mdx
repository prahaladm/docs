---
title: "Skills"
description: "Reusable skill definitions that guide an agent's use of the NativeFlow MCP tools."
---

This page should cover skills: packaged instructions (like `read_skill`/`search_skills`) that teach an agent the right order of operations for a task — e.g. always fetching design system foundations before creating a component.

<Note>
  TODO: list the built-in skills and when each should load.
</Note>