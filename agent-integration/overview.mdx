---
title: "Overview"
description: "How AI agents like Claude, Cursor, and Copilot build NativeFlow apps through the Studio MCP server."
---

This page should introduce agent-driven authoring: what the NativeFlow Studio MCP server exposes, how it differs from a generic code-generation assistant (it edits the same structured document model Studio uses), and which agents are supported today.

<Note>
  TODO: add a short capability matrix of what's currently agent-buildable end to end versus what still needs a human in Studio.
</Note>